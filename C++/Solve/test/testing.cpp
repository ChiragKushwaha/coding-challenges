#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    // char c ='A';
    // char ans = c| ~(' ');
    // char ch = 'a';
    // char ans1 = ch & '_';
    // cout<< ans<<endl;
    // int x =32;
    // cout<<int(x&(x-1))<<endl;
    // cout<<
    // for (int i=0; i<5; i++) {
    //     cout<<(1<<i)<<endl;
    // }

    // for (int n=10;n<=500;n+=10) {
    // cout<<n<<endl;
    // double k = floor(log10(5242)) + 1;
    // cout << "k : " << k << endl;
    // cout<<"log : "<<k<<endl;
    // cout<<"floor(k) : "<<floor(k)<<endl;
    // k= k - floor(k);
    // cout<<"k - floor(k) : "<<k<<endl;
    // int x = pow(10, k);
    // cout<<"pow : "<<x<<endl;
    // cout<<"****************************"<<endl;
    // }
    // int N = 3*3*3*3, K = 4;
    // int res1 = log(N) / log(K);
    // double res2 = log(N) / log(K);
    // cout <<  (res2 == double(res1));
    cout << __gcd(36, 60) << endl;
    return 0;
}